# 开源快照验证边界

核验日期：2026-09-21。以下结果来自独立开源目录，不代表安装到任意设备后的兼容保证。

| 对象 | 结果 |
| --- | --- |
| Swift 默认回归 | 158 项：156 通过、2 项系统窗口焦点用例明确跳过、0 失败 |
| Node 中转协议 | 17 项通过，包含二进制 / JSON 兼容、错误鉴权、密文与回执流程 |
| 打包脚本 | 6 项通过，包含输出保护与签名配置边界 |
| 开源目录边界 | 5 项通过：独立 App、共用源码、权限声明、用户配置中转地址、图标资源 |
| Mac 开发包 | Release 构建及临时签名校验通过；未安装或启动，未做公证 |
| 独立 iOS App | 真机架构 Release 未签名构建通过；包名与两个 AppIntent 元数据已核对 |
| 独立 iOS 真机端到端 | 未安装与重测；旧容器中的模块验收不能代替本项 |
| 发布文件检查 | 从白名单生成新目录，检查实际 Git 暂存内容、私密文件类型、已知敏感模式与 PNG 元数据；未发现待处理命中 |

本机使用 Xcode 26.6、Swift 6.3.3、Node.js 25.8.2。GitHub Actions 配置另在 Node.js 22 和 macOS 26 runner 上执行，实际结果以 Actions 页面对应提交为准。

## 没有掩盖的测试差异

首次完整运行有 3 项失败：一项测试把点尺寸误当作固定像素尺寸，夹具现已改为明确 300 × 400 像素；另外两项读取操作系统 key window，在普通 SwiftPM 测试宿主中无法完成可靠焦点验收。

焦点测试保留原有断言，仅改为显式交互验收入口；不是删除测试、放宽断言或宣称问题已修复。启用方法与限制见 [贡献指南](../CONTRIBUTING.md)。回车事件、复制目标、元数据及其他默认回归照常执行。

本次没有改现用 App 的安装、配对、权限或服务器。文件扫描只用于发布卫生检查，不是独立安全审计，也不能证明不存在未知风险。
